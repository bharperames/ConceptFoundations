{
  "schema_version": 1,
  "consumer": "ConceptFoundations",
  "produces_for": "MR_AudioClips",
  "about": "The consumer's git-tracked, human-authored demand ledger — phrases the app needs that the GOLD catalog does not have yet. `scripts/register_demand.py` PUSHES these into the shared catalog's `requests` table (assets.db), where the curator reads them and replies via `fulfilled_by`. This JSON is the authored source; the DB row is the shared interop surface, written only by that tool (single writer per column → no drift). See docs/audio-clip-contract.md §8 and docs/handoff-demand-tables.md.",
  "invariants": [
    "This file lists ONLY open requests. Presence here == an open row in assets.db `requests`.",
    "Fulfillment is migration, not a flag. The producer sets `requests.fulfilled_by` on promotion; the consumer then wires the asset_id into clips/clips.manifest.json, adds a `usage` row, and DELETES the request from here (which deletes the DB row on the next register-demand). 'Done' == 'gone'.",
    "In-use dependencies are NOT authored here. clips/clips.manifest.json is the authoritative list of asset_ids we ship; register_demand.py mirrors it into the `usage` table for the 'don't de-curate these' signal.",
    "Run `make register-demand` to push this ledger into assets.db and read back fulfilled_by (OPEN / READY / STALE)."
  ],
  "requests": [
    {
      "phrase": "The itsy bitsy spider climbed up the water spout!",
      "norm": "the itsy bitsy spider climbed up the water spout",
      "used_in": ["causality/7.x — the spider climbs the spout (rhyme line 1 of 2)"],
      "priority": "medium",
      "currently": "tts",
      "constraints": { "max_duration_s": 3.5, "has_music": "either", "voice": "warm nursery-rhyme reading; should pair with the line-2 clip below as one couplet" },
      "candidate_asset_ids": [],
      "note": "Line 1 of the itsy-bitsy-spider couplet, spoken as the spider climbs. Producer surfaced the line-2 clip from raw_audio/itsybitsy.mp3 — the matching line-1 (\"The itsy bitsy spider climbed/went up the water spout\") is very likely in the same recording just before it (before 6.58s). Promote it so the two lines share one voice. Until then it's TTS. On promotion I add the norm -> mp3 to CLIP_MAP (one line)."
    },
    {
      "phrase": "Down came the rain and washed the spider out!",
      "norm": "down came the rain and washed the spider out",
      "used_in": ["causality/7.x — rain falls and washes the spider out (rhyme line 2 — one clip covers the whole beat)"],
      "priority": "high",
      "currently": "tts",
      "constraints": { "max_duration_s": 5.0, "has_music": "either", "voice": "same voice as line 1" },
      "candidate_asset_ids": [],
      "note": "SUPERSEDES the earlier three fragment requests (Up!/Down!/Out! and then Down-came-the-rain! / And-washed-it-out!). The producer surfaced the COMPLETE line as one clip: silver clip_df618c4a15b8 — \"Down came the rain and washed the spider out.\" 4.60s, conf 99%, music, raw_audio/itsybitsy.mp3 @ 6.58-11.18s. Please promote clip_df618c4a15b8 to gold. The app now speaks this as ONE utterance while the rain falls and the spider washes out, so the single clip voices the whole beat. Note on timing: the app currently triggers the wash-out ~1.7s into the line (tuned for TTS); on promotion I'll retime the wash-out to land on the \"washed ... out\" portion of the 4.6s clip (~2.9s in). Wiring is a one-line CLIP_MAP add (norm -> down_came_out.mp3) + a manifest entry."
    },
    {
      "phrase": "you did it",
      "norm": "you did it",
      "used_in": ["mini-games/Picture Puzzle — each picture built, and the all-three finale"],
      "priority": "medium",
      "currently": "tts",
      "constraints": { "max_duration_s": 1.6, "has_music": "either", "voice": "warm child-facing praise (second person)" },
      "candidate_asset_ids": ["asset_98cf57f963af"],
      "note": "Second-person 'You did it!' for the Picture Puzzle win. If no good 'you did it' recording exists, fulfil with the 'we did it' take — silver clip_ed372817a473 = gold asset_98cf57f963af (I already ship it as we_did_it.mp3). Producer's call. Once fulfilled I add 'you did it' → you_did_it.mp3 to CLIP_MAP and the existing wording upgrades from TTS to the clip with no reword."
    }
  ]
}
